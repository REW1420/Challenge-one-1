package Model;

public interface ConversonInterface {
    double principalToYuang(double principal);
    double principalToEuro(double principal);
    double principalToLibra(double principal);
    double principalToYen(double principal);
    double principalToWon(double principal);
    double anyToUSD(int key, double principal);
}
